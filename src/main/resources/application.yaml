spring:
  application:
    name: spring-ai-claude-mcp
  ai:
    mcp:
      server:
        name: spring-ai-claude-mcp
        version: 0.0.1
  main:
    web-application-type: NONE
    # NOTE: You must disable the banner and the console logging
    # to allow the STDIO transport to work !!!
    banner-mode: OFF
logging:
  level:
    root: OFF
#  pattern:
#    console: "%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n"
#  file:
#    name: logs/bruno.log